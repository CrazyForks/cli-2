name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  check:
    name: Verify tag and package
    runs-on: ubuntu-latest
    outputs:
      dist-tag: ${{ steps.version.outputs.dist-tag }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - run: npm ci
      - run: npm audit --audit-level=moderate
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test

      # Pre-release versions (1.2.3-rc.1) publish under the `next` dist-tag
      # so `npm i -g @wdl-dev/cli` keeps resolving to the last stable release.
      - name: Tag must match package.json version
        id: version
        run: |
          version="$(node -p 'require("./package.json").version')"
          if [ "v${version}" != "${GITHUB_REF_NAME}" ]; then
            echo "tag ${GITHUB_REF_NAME} does not match package version v${version}" >&2
            exit 1
          fi
          case "$version" in
            *-*) echo "dist-tag=next" >> "$GITHUB_OUTPUT" ;;
            *) echo "dist-tag=latest" >> "$GITHUB_OUTPUT" ;;
          esac

      - run: npm pack --dry-run

  publish-npmjs:
    name: Publish to npmjs
    needs: check
    # npm provenance requires a public source repository; while this repo is
    # private, rehearse releases against GitHub Packages only.
    if: ${{ !github.event.repository.private }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # provenance attestation
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: https://registry.npmjs.org

      # --ignore-scripts: the check job already gated this commit;
      # prepublishOnly guards manual publishes, not CI. No install needed —
      # the tarball is packed from the files allowlist with no build step.
      #
      # NPM_TOKEN is a granular automation token for the wdl-dev org.
      # Tokenless alternative: configure npm trusted publishing (OIDC) for
      # this repo + workflow on npmjs, upgrade npm to >= 11.5 in this job,
      # and drop the NODE_AUTH_TOKEN line.
      - run: npm publish --ignore-scripts --provenance --access public --tag "$NPM_DIST_TAG"
        env:
          NPM_DIST_TAG: ${{ needs.check.outputs.dist-tag }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-github:
    name: Publish to GitHub Packages
    needs: check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: https://npm.pkg.github.com
          scope: "@wdl-dev"

      - run: npm publish --ignore-scripts --tag "$NPM_DIST_TAG"
        env:
          NPM_DIST_TAG: ${{ needs.check.outputs.dist-tag }}
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  github-release:
    name: Create GitHub Release
    # The npmjs job legitimately skips while the repository is private, so
    # gate on "nothing failed" rather than on every job succeeding.
    needs: [check, publish-npmjs, publish-github]
    if: ${{ !failure() && !cancelled() }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      # Final releases take their notes from the matching CHANGELOG.md
      # section; pre-releases (x.y.z-rc.N) have no section and fall back to
      # generated notes, marked Pre-release so they never show as Latest.
      - name: Create the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${GITHUB_REF_NAME#v}"
          awk -v ver="$version" '
            $0 == "## " ver { found=1; next }
            /^## / && found { exit }
            found { print }
          ' CHANGELOG.md > /tmp/notes.md
          args=(--verify-tag)
          if [ -s /tmp/notes.md ]; then
            args+=(--notes-file /tmp/notes.md)
          else
            args+=(--generate-notes)
          fi
          case "$version" in
            *-*) args+=(--prerelease) ;;
          esac
          gh release create "$GITHUB_REF_NAME" "${args[@]}"
